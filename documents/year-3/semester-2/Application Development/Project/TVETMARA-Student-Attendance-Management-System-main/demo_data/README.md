# Demo timetable CSV files

Use the scoped CSV files for role testing:

- `clean_no_conflict_timetable_ELEKTRIK_JAN_JUN_2026.csv` is for `kj_elektrik@tvetmara.edu.my` and contains only DED, DCP, and DCB rows.
- `clean_no_conflict_timetable_DGS_JAN_JUN_2026.csv` is for `kp_dgs@tvetmara.edu.my` and contains only DGS rows.

The legacy `clean_no_conflict_timetable_JAN_JUN_2026.csv` is retained for compatibility with older tests, but it is a mixed-scope demo file and should not be used as a KJ Elektrik-only upload.
